apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "membership-api-chart.fullname" . }}-config
  labels:
    {{- include "membership-api-chart.labels" . | nindent 4 }}

data:
  DB_URL: {{ .Values.database.url | quote }}