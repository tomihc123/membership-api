apiVersion: v1
kind: Service
metadata:
  name: {{ include "membership-api-chart.fullname" . }}
  labels:
    {{- include "membership-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "membership-api-chart.selectorLabels" . | nindent 4 }}
